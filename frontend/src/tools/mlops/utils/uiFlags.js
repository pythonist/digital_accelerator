// Shared UI behavior flags for the MLOps workbench.
// Keep ASCII only.

// If true, UI actions remain clickable even when prerequisites are missing.
export const ALLOW_INCOMPLETE_ACTIONS = false;

// If true, show guard banners that explain missing prerequisites per step.
export const SHOW_STEP_GUARDS = true;
