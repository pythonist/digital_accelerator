apiVersion: v1
kind: ConfigMap
metadata:
  name: fcc-workbench-config
data:
  AML_BACKEND_PROFILE: "full"
  AML_AUTO_BOOTSTRAP_VENV: "0"
  APP_PORT: "5000"
  PORT: "5000"
  WEB_CONCURRENCY: "4"
  GUNICORN_THREADS: "8"
  GUNICORN_TIMEOUT: "600"
  GUNICORN_GRACEFUL_TIMEOUT: "60"
  GUNICORN_KEEPALIVE: "30"
